{
  "name": "2800-202610-bby03",
  "private": true,
  "scripts": {
    "install:client": "cd public && npm install",
    "install:server": "cd server && npm install",
    "install:all": "npm run install:client && npm run install:server",
    "build:client": "cd public && npm run build",
    "build": "npm run install:all && npm run build:client",
    "dev:client": "cd public && npm run dev",
    "dev:server": "cd server && npm run dev",
    "start": "cd server && node --import tsx index.ts"
  }
}
